### Yambar status bar

# Use git version to handle display powering off and on
# Replace at next release
<% if [ $(hostname) = "fedora" ]; then %>
exec_always sh -c "killall yambar; /home/struan/Development/Utilities/yambar/bld/release/yambar &"
<% elif [ $(hostname) = "archlaptop" ]; then %>
exec_always sh -c "killall yambar; yambar &"
<% fi -%>
