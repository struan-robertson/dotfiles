# QT Theming
QT_QPA_PLATFORMTHEME=qt5ct

# GTK Theming
GTK_THEME=Nordic
ICON_THEME=NovaOS-nord
XCURSOR_SIZE=24

XDG_CONFIG_HOME=$HOME/.config
XDG_SCREENSHOTS_DIR=$HOME/Pictures/Screenshots

# SSH agent auth socket
SSH_AUTH_SOCK=$XDG_RUNTIME_DIR/ssh-agent.socket

EDITOR="mg"
VISUAL="emacsclient -c"

# hledger file
LEDGER_FILE="$HOME/Documents/Keys/Financial/2025.ledger"

# runit user service directory
SVDIR=$HOME/.local/service

# Update PATH
PATH=$HOME/.local/bin:$HOME/.cargo/bin:$PATH

# Flatpak dirs
# XDG_DATA_DIRS=/var/lib/flatpak/exports/share:$HOME/.local/share/flatpak/exports/share:$XDG_DATA_DIRS

# IPython home directory
IPYTHONDIR="$HOME/.config/ipython"