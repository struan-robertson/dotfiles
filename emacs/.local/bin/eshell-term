#!/bin/sh

case "${1:-}" in
    --new)
        shift
        path="${1:-$(pwd)}"
        emacsclient -c -n -e "(my/eshell-term \"$path\" t)"
        ;;
    ""|*)
        path="${1:-$(pwd)}"
        emacsclient -c -n -e "(my/eshell-term \"$path\")"
        ;;
esac
